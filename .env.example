# 모든 값은 로컬 환경 또는 CI Secret에서만 설정합니다. 실제 값은 커밋하지 않습니다.
# DISCORD_WEBHOOK_URL=
# Cloudflare/GitHub Action에서는 Secret으로 설정합니다. 로컬 테스트에서만 사용합니다.
# OPENAI_API_KEY=
# RAG_EMBEDDING_PROVIDER=local
# PUBLIC_SITE_URL=https://openkiki.org
